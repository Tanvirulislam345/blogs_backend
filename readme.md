```
1. npm init -y
2. tsc --init
3. tsconfig.json file target will be updated
3. npm install express mongoose typescript ts-node-dev dotenv bcryptjs jsonwebtoken zod
4. npm install @types/express @types/node @types/bcryptjs @types/jsonwebtoken -D


```
